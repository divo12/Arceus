[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 600
preDeployCommand = "npm run db:migrate --workspace @arceus/db && npm run db:seed-users --workspace @arceus/db"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
