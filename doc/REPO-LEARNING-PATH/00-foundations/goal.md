# `doc/GOAL.md`

Why it matters:

- This is the shortest statement of why the repo exists.
- It tells you the problem Paperclip thinks is worth solving.

Read focus:

- Product pain being addressed.
- Why an agent company needs a control plane.
- What is intentionally in scope versus out of scope.

Connections:

- `PRODUCT.md` turns the goal into domain concepts.
- `SPEC-implementation.md` turns the goal into build commitments.

Questions:

- What human problem is Paperclip solving?
- Why is “control plane” a better phrase than “agent app” here?
- What would the system lose if this goal changed?

