# `doc/SPEC-implementation.md`

Why it matters:

- This is the build contract for the current version.
- Use it to separate shipped expectations from future ambition.

Read focus:

- Required behavior for V1.
- Explicit invariants and product promises.
- Areas where the code must stay aligned with spec.

Connections:

- Route and service behavior should match this file.
- Good review question: “does this code path satisfy the implementation spec?”

Questions:

- What is mandatory in V1?
- What behavior in the repo exists to satisfy this spec directly?
- Which future ideas are intentionally deferred?

