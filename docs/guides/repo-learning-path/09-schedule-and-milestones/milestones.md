# Learning Milestones

You have crossed the first real milestone when you can do these without guessing:

1. Explain `server/src/index.ts` startup flow in plain English.
2. Point at where company access is enforced for backend routes.
3. Trace one agent action from UI event to DB or runtime effect.
4. Explain the difference between a wakeup request and a heartbeat run.
5. Explain the difference between `packages/shared` and `packages/db`.
6. Explain where memory plugs into execution, and where it does not.
7. Explain the difference between role definitions, hierarchy, delegation, and spawning.

If you can do all seven, you do not merely “recognize files” anymore.

You have a working systems model of the repo.
