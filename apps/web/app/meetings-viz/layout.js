export const metadata = {
    title: "Meeting Room — Arceus",
    description: "Round table meeting visualization",
};
export default function MeetingsVizLayout({ children }) {
    return children;
}
