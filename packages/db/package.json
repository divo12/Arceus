{
  "name": "@arceus/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun src/scripts/apply-migrations.ts",
    "db:migrate:raw": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:lint-migrations": "bun src/scripts/lint-migrations.ts",
    "db:seed": "bun src/seed.ts",
    "db:explain-audit": "bun src/scripts/explain-audit.ts",
    "db:backfill-memory": "bun src/scripts/backfill-memory.ts"
  },
  "dependencies": {
    "@arceus/contracts": "file:../contracts",
    "@supabase/supabase-js": "^2.56.0",
    "drizzle-orm": "^0.44.5",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.4"
  }
}