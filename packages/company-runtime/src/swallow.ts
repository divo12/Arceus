/**
 * Re-export shim — swallowAndAudit moved to @arceus/runtime-shared in
 * Spec 34 v3 PR 14. Existing imports (`./swallow.js`) keep working.
 */
export { swallowAndAudit } from "@arceus/runtime-shared";
