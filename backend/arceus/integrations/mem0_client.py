"""Mem0 self-hosted client wrapper."""

from arceus.config.settings import settings

# TODO: implement Mem0 client
# - Connect to self-hosted Mem0 instance at settings.mem0_url
# - Map Hippocampus model to Mem0's multi-level API
# - store(), retrieve(), search(), consolidate()
# - Mem0 manages its own vector DB under the hood
